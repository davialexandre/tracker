name: tracker

services:
  php:
    build: .
    ports:
      - "9899:80"
    volumes:
      - ./web:/var/www/html
      - ./src:/var/www/src
      - ./scripts:/var/www/scripts
      - data:/var/tracker/data

volumes:
  data: {}
